sudo: false
language: node_js
node_js: stable

install: npm install

stages:
  - compile
  - test
  - name: deploy
    if: branch = master

jobs:
  include:
    - stage: compile
      script: npm run build:clean
    - stage: test
      script: npm test
