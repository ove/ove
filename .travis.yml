sudo: false
language: node_js
node_js: stable

services:
  - docker

install: npm install

stages:
  - test
  - name: deploy
    #    if: branch = master

jobs:
  include:
    - stage: test
      script: npm run lint && npm run build:clean && npm test
    - stage: deploy
      script: ./build.sh
